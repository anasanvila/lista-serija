import { Props } from "framer-motion/types/types"
import {Text} from '@chakra-ui/react'

const ShowListPage = (props:Props) => {
    return <Text color={props.color}>show list</Text>
}

export default ShowListPage